# Secrets — never commit
.env
.env.local
.env.*.local
*.key
*.pem
*.pickle
token.pickle
client_secrets.json
service_account.json
# Forensic copy of the broken service-account upload script
youtube_upload.py.service_account_attempt

# Python
.venv/
__pycache__/
*.pyc

# macOS noise
.DS_Store
Icon?

# Claude Code worktrees + project state
.claude/

# Per-episode generated content — ALL of episodes/ is reproducible from
# code + topics queues, so we don't commit any of it. Keep code only.
episodes/

# Runtime logs
logs/

# TMDB cache from older runs (now lives under episodes/<slug>/tmdb/)
tmdb_cache/

# Anything in /tmp paths
*.tmp
